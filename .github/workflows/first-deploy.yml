name : my-first-action

on : push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "My First github actions"
      - name: List of files
        run: ls
        